MAX_BOT_TOKEN
MAX_CHAT_ID
MAX_PREVIEW_CHAT_ID
TELEGRAM_BOT_TOKEN
TELEGRAM_CHANNEL_CHAT_IDS=@nmorozova_emdr,@natalia_morozova_psy
TELEGRAM_CHANNEL_UTM_SOURCES=tg1,tg2
# ⚠️ Канал @morozova_emdr снят с публикации 2026-08-03. Не включать в TELEGRAM_CHANNEL_CHAT_IDS.
# Gate: materialize_cloud_env.py + cloud_preflight.py + send-telegram-post.py блокируют любой другой список.
# Синхронизировать Cloud Secrets и VPS telegram.env.local с profile/telegram-posts-registry.md.
# VPS: задать TELEGRAM_* в systemd EnvironmentFile — publish-browser-deferred / vps-webhook-server
# materialize telegram.env.local при старте (rsync/git не трогают *.env.local).
ZERNIO_API_KEY
ZERNIO_FACEBOOK_ACCOUNT_ID
RUNWARE_API_KEY
GRSAI_API_KEY
# GRSAI: тексты (gemini-3.1-pro) + обложки (gpt-image-2), опционально GRSAI_CHAT_MODEL / GRSAI_COVER_MODEL
KIE_API_KEY
FTP_SERVER
FTP_USERNAME
FTP_PASSWORD
FTP_SERVER_DIR

# WordPress Media Library — основное хранилище обложек (требуется для b17 + VK/FB):
# WordPress backend для morozovanatalia.ru — morozova-natalya.ru
WORDPRESS_URL=https://morozova-natalya.ru
WORDPRESS_USER=
WORDPRESS_APP_PASSWORD=

# Для webhook после push (фаза 3):
VPS_WEBHOOK_SECRET
# Проверка совпадения Cloud ↔ VPS (без публикации):
# VPS_WEBHOOK_SECRET='...' python3 scripts/verify-vps-webhook-secret.py

# VK_GROUP_ID=224685309 — только если materialize; иначе default в handoff

# OK группа (MCP ok_create_post_with_photo):
# OK_GROUP_GID=70000034253679

# Фаза 3 Linux VPS — secrets только на VPS (не в cloud):
# BROWSER_BACKEND=playwright
# PLAYWRIGHT_STORAGE_STATE — файл cookies на VPS
# B17_PROXY_SERVER — residential RU proxy (обязательно для b17)
# GITHUB_TOKEN — git pull private repo (github.env.local)
# VPS_WEBHOOK_SECRET — webhook из cloud automation

# Фаза 3 Windows/remote Undetectable (устарело) — опционально:
UNDETECTABLE_BASE_URL
UNDETECTABLE_PROFILE_ID
UNDETECTABLE_API_BEARER

# Фаза 3 режим B (VPS worker) — secrets НЕ в cloud; browser.env.local на Linux VPS
